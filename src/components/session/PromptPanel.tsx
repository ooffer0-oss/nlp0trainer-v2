import { Placeholder } from '@/components/common/Placeholder'

/**
 * Will render the current exercise prompt. Content and prompt
 * types are not yet designed — placeholder only.
 */
export function PromptPanel() {
  return <Placeholder label="Prompt panel — not implemented" className="min-h-32" />
}
