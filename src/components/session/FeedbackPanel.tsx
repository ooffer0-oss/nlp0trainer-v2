import { Placeholder } from '@/components/common/Placeholder'

/**
 * Will render evaluation feedback. No AI/NLP evaluation is wired up —
 * placeholder only.
 */
export function FeedbackPanel() {
  return <Placeholder label="Feedback panel — not implemented" className="min-h-24" />
}
