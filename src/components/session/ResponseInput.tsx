import { Placeholder } from '@/components/common/Placeholder'

/**
 * Will capture the learner's response. Input mode (text/audio/choice)
 * is not yet decided — placeholder only.
 */
export function ResponseInput() {
  return <Placeholder label="Response input — not implemented" className="min-h-24" />
}
